import { useTheme } from '../../shared/theme';
import { Card, Badge } from '../../shared/ui';
export function Settings({ model, transport }: { model: string; transport: { type: string; channel?: string; port?: string } }) { const { theme, toggle } = useTheme(); const endpoint = transport.type === 'can' ? transport.channel : transport.port; return <div className="stack"><div className="page-heading"><div><p className="eyebrow">工作区 / 设置</p><h1>设置</h1><p>调整工作区偏好与设备连接信息。</p></div></div><Card><div className="settings-row"><div><h2>外观主题</h2><p className="muted">当前：{theme === 'light' ? '浅色' : '深色'} · 颜色过渡约 180ms</p></div><button className="button button-secondary" onClick={toggle} aria-label="切换主题">切换为{theme === 'light' ? '深色' : '浅色'}</button></div><div className="settings-row"><div><h2>当前设备</h2><p className="muted">{model} · {endpoint}</p></div><Badge tone="green">已连接</Badge></div><div className="settings-row"><div><h2>动效</h2><p className="muted">尊重系统的减少动效偏好</p></div><Badge>自动</Badge></div></Card></div>; }
