# app-runtime

Typed coordinator for device, motion, telemetry, action, grasp, and
cancellation ports. The `ui` module provides UI-facing facade traits using the
canonical DTOs; `SidecarPort` remains an internal stop/unlock dependency.
